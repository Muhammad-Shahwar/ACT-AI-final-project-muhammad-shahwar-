'use client';
import { useState } from 'react';
import { Star } from 'lucide-react';
export function Feedback() {
  const [rating,setRating]=useState(5); const [message,setMessage]=useState(''); const [loading,setLoading]=useState(false);
  async function submit(e: React.FormEvent<HTMLFormElement>) { e.preventDefault(); setLoading(true); setMessage(''); const fd=new FormData(e.currentTarget); try { const res=await fetch('/api/feedback',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:fd.get('name'),rating,comment:fd.get('comment')})}); if(!res.ok) throw new Error(); setMessage('Thank you! Your feedback has been received.'); e.currentTarget.reset(); setRating(5); } catch { setMessage('Something went wrong. Please try again.'); } finally { setLoading(false); } }
  return <section id="feedback" className="mx-auto max-w-3xl px-4 py-20 sm:px-6"><div className="rounded-3xl bg-white p-6 shadow-soft sm:p-10"><h2 className="text-3xl font-black text-gray-900">Share your feedback</h2><p className="mt-2 text-gray-600">Help us make healthier restaurant choices easier.</p><form onSubmit={submit} className="mt-8 space-y-5"><input required name="name" placeholder="Your name" className="w-full rounded-2xl border border-gray-200 px-4 py-3 outline-none focus:border-orange-500"/><div><label className="mb-2 block text-sm font-bold text-gray-700">Rating</label><div className="flex gap-2">{[1,2,3,4,5].map(n=><button type="button" key={n} onClick={()=>setRating(n)} aria-label={`${n} stars`}><Star className={n<=rating?'fill-amber-400 text-amber-400':'text-gray-300'}/></button>)}</div></div><textarea required name="comment" rows={4} placeholder="Tell us what you liked or what we can improve" className="w-full rounded-2xl border border-gray-200 px-4 py-3 outline-none focus:border-orange-500"/><button disabled={loading} className="rounded-full bg-orange-600 px-7 py-3 font-bold text-white hover:bg-orange-700 disabled:opacity-60">{loading?'Submitting...':'Submit Feedback'}</button>{message&&<p role="status" className="font-semibold text-green-700">{message}</p>}</form></div></section>;
}
